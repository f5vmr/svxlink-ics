stage 1 - with the image installed and card attached
 ./svxlinkbuilder/preinstall
 first menu What card have you attached
 	1.1X
 	2.2X
 	3.4X
 for options 1 and 2 we are installing a single MCP, so we can use the same code for the small dts
 
 for option 3. we use the large dts.
 
procedure small 
 	take the content of expander.small.txt
    call compiler
    
procedure large
    take the content of expander large.txt    
    call compiler

compiler
    write to /tmp/ics-repeater.dts
    call compiler process to make ics-repeater.dtbo
    sudo dtc -@ -I dts -O dtb -o /boot/overlays/ics-repeater.dtbo /tmp/ics-repeater.dts
    add to /boot/firmware/config.txt
    dtoverlay=i2c_arm=on
    dtoverlay=12s=on
    dtoverlay=ics-repeater

